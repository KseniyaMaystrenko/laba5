# Генератор простых чисел. Просуммируйте возвращаемые числа.
# К генератору должна быть применена хотя бы одна из функций map, reduce, filter

import random #Подключаем модуль генерации чисел
from functools import reduce #Подключаем функцию reduce из модуля functools
x=int(input('Введите сколько значений сгенерировать: '))
a=int(input('Введите начальный диапазон: '))
b=int(input('Введите конечный диапазон: '))
kape=[] #Создаем пустой список для случайных значений
for i in range(x): #Цикл в котором мы добавляем случайные числа в список
    kape.append(random.randint(a,b)) #Добавляем в список элемент, значение котрого равно случайному числу в диапазоне [a..b]
    print('[',i+1,']:',kape[i],sep="") #Выводим добавленный элемент на экран
sum=reduce(lambda x,y: x + y, kape) #Вычисление суммы всех элементов списка при помощи reduce() и lambda-функции:
print('Сумма сгенерированных чисел -',sum) #Выводим сумму элементов списка на экран
